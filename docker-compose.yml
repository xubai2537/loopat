services:
  loopat:
    build: .
    image: ghcr.io/simpx/loopat:latest
    privileged: true
    ports:
      - "20001:10001"
      - "17788:7788"
    volumes:
      - loopat-data:/home/loopat/.loopat
      # - ~/.ssh:/home/loopat/.ssh:ro  # 可选：挂载 SSH 密钥用于 git 操作
    environment:
      - LOOPAT_HOME=/home/loopat/.loopat
      - HOST=0.0.0.0
      - LOOPAT_SERVE_HOST=0.0.0.0
      # External Runtime Gateway SSE — tokens are managed per-user via
      # Settings > Gateway Tokens in the loopat UI. No env vars needed.
    restart: unless-stopped

volumes:
  loopat-data:
