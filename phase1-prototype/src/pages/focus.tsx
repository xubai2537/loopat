export function FocusPage() {
  return (
    <div class="h-full w-full p-8 text-sm text-gray-500">
      <h2 class="text-lg font-medium text-gray-900 mb-2">Focus</h2>
      <p>当下注意力 view —— 待补完整 mock（pinned / 8d-expire / m:n loop 关联）。</p>
    </div>
  )
}
